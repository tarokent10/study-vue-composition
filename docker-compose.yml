version: "3.9"
services:
  web:
    image: nginx:1.20-alpine
    ports:
      - 8080:80
    volumes:
      - ./dist:/work
      - ./infra/nginx/default.conf:/etc/nginx/conf.d/default.conf
    working_dir: /work
